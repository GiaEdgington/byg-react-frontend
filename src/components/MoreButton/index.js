import React from 'react';


const MoreButton = ({onClick}) => {
  
    return (
        <button 
        type="button"
        onClick = {onClick}
        >More
        </button>
    )
}

export default MoreButton;